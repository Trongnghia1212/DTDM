@model IEnumerable<QLNhanSu.Models.Phong>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}

<h2>Phòng Ban</h2>

<p>
    @Html.ActionLink("Thêm mới", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TenPhong)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TenPhong)
        </td>
        <td>
            @Html.ActionLink("Sửa", "Edit", new { id=item.MaPhong }) |
            @Html.ActionLink("Chi Tiết", "Details", new { id=item.MaPhong }) |
            @Html.ActionLink("Xóa", "Delete", new { id=item.MaPhong })
        </td>
    </tr>
}

</table>
